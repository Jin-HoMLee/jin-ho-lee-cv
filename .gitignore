# PII overlay — never committed
content.private/

# Private headshot for --photo PDF build (German anti-discrimination convention: PDF default is photo-less)
assets/photo.*

# Cover letters — per-application material is private, never committed (this repo is public)
applications/

# Handwritten signature image for cover-letter PDFs (optional, like the photo)
assets/signature.*

# Build outputs
dist/
dist-private/

# Python
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.venv/
*.egg-info/

# Node (Phase 3+)
node_modules/

# Phase 3 — Astro website
web/dist/
web/src/data/*.json
web/.astro/

# OS
.DS_Store

# Claude Code session state + brainstorming mockups (keep skills/ committed)
.claude/*
!.claude/skills/
.superpowers/

# claude-personas embedded topology (see .agents/manifest):
# memory is private (this repo is public); .codex/hooks.json is
# doctor-generated with per-clone absolute paths
.agents/memory/
.codex/

# PDF build cache (JSON intermediate written by pdf/build.py)
pdf/.cache/

# Phase 12 digital-twin Worker
worker/node_modules/
worker/.wrangler/
worker/chat-context.md
worker/.dev.vars
worker/.dev.vars.*
!worker/.dev.vars.example

# Phase 13 — master-cv/ overlay (gitignored superset feeding the twin; example template stays committed)
master-cv/
